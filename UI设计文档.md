# 领益工作助手 · 全场景 UI 设计文档

> 版本 v3.0 · 苹果设计语言 · Windows 桌面侧边栏
> 2026-08-10

---

## 1. 设计概述

### 1.1 产品定位

领益工作助手是一个常驻 Windows 系统托盘的桌面工具，提供三项核心能力：

- **备忘**：记录待办、支持自然语言快速创建与拖拽排序
- **剪切板**：历史记录、固定、批量管理
- **邮件**：公司 Exchange 邮箱连接、出货邮件拉取与详情解析

窗口形态为 **400 × 640 无边框侧边栏浮窗**，常驻屏幕右上角，支持贴边收纳、鼠标穿透、迷你模式。

### 1.2 设计语言

整体采用 **苹果设计语言（Apple Design Language）**：

| 维度 | 规范 |
| --- | --- |
| 强调色 | iOS 系统蓝 `#0A84FF` |
| 材质 | 半透明毛玻璃 `backdrop-filter: blur(22px) saturate(180%)` |
| 边框 | 发丝描边 `1px rgba(0,0,0,0.08)` |
| 圆角 | 控件 10px / 卡片 14px / 弹层 18px / 窗口 22px |
| 动效 | `cubic-bezier(0.32, 0.72, 0, 1)`，微交互 150ms / 面板 250ms / 大面板 350ms |
| 字体 | `-apple-system / SF Pro / Segoe UI Variable / PingFang SC` |

---

## 2. 设计 Token

### 2.1 色彩体系（浅色 / 深色）

| Token | 浅色 | 深色 | 用途 |
| --- | --- | --- | --- |
| `--brand-500` | `#0a84ff` | `#0a84ff` | 主强调色 |
| `--text` | `#1d1d1f` | `#f5f5f7` | 主文本 |
| `--text-muted` | `rgba(60,60,67,0.6)` | `rgba(235,235,245,0.6)` | 次级文本 |
| `--bg-window` | `rgba(245,245,247,0.82)` | `rgba(28,28,30,0.82)` | 窗口毛玻璃底 |
| `--bg-card` | `rgba(255,255,255,0.72)` | `rgba(44,44,46,0.75)` | 卡片 |
| `--border` | `rgba(0,0,0,0.08)` | `rgba(255,255,255,0.12)` | 发丝描边 |
| `--success` | `#34c759` | `#30d158` | 成功（系统绿） |
| `--danger` | `#ff3b30` | `#ff453a` | 危险（系统红） |

### 2.2 便签色板

`红 #ff3b30 / 橙 #ff9f0a / 黄 #ffd60a / 绿 #30d158 / 蓝 #0a84ff / 紫 #bf5af2`

### 2.3 圆角 / 阴影 / 动效

- 圆角：`--radius-sm 10px`（控件）、`--radius-md 14px`（卡片）、`--radius-lg 18px`（弹层）、`--radius-xl 22px`（窗口）
- 阴影：`--shadow-sm 0 2px 8px / --shadow-md 0 6px 20px / --shadow-lg 0 14px 34px / --shadow-xl 0 22px 54px`
- 动效时长：微交互 100-160ms、下拉/弹层 150-250ms、面板/抽屉 200-350ms；所有动效支持 `prefers-reduced-motion` 降级

---

## 3. 核心组件规范

### 3.1 窗口壳层

```
┌────────────────────────────┐
│ app-header    (拖拽区)      │  16px 上下留白，品牌点 + 标题 + 操作钮
├────────────────────────────┤
│ toolbar + category-chips   │  搜索胶囊 + 分类筛选行
├────────────────────────────┤
│ 内容区 (flex:1, 滚动)       │  16px 左右留白，卡片间距 6px
├────────────────────────────┤
│ app-tabbar (毛玻璃悬浮)     │  剪切板 / 邮件 / 便签 / 设置
└────────────────────────────┘
```

### 3.2 按钮

| 类型 | 规范 |
| --- | --- |
| 主按钮 | 胶囊形 `999px`，品牌蓝底白字，`:active` `scale(0.97)` |
| 次按钮 | 发丝描边 + 透明底，hover 品牌软底 |
| 图标按钮 | 32×32 圆角 10px，hover 品牌软底 + `scale(1.06)` |
| 危险按钮 | 系统红底，hover 加深 |

### 3.3 便签卡片

- 发丝描边 + 圆角 14px + 白毛玻璃底
- 结构：完成勾选 / 置顶钉 / 标题 / 时间 / 双行预览 / 分类胶囊 + 附件计数
- 交互：hover 上浮 `translateY(-1px)` + 边框品牌色；拖拽时 `scale(1.02)` + 深阴影；让位卡片虚线描边
- 行级操作：置顶按钮默认隐藏，hover 揭示

### 3.4 剪切板卡片

- 类型标签 + 相对时间 + 双行预览（`line-clamp:2`）
- 固定卡片左侧 3px 品牌蓝边
- hover 揭示固定/删除操作

### 3.5 邮件卡片

- 主题 + 相对时间 + 发件人 + 双行正文预览
- 未读卡片左侧 2px 品牌蓝边

### 3.6 空状态

统一结构：48px 圆角方块图标容器（凹面底）→ 标题 → 说明 → 可选操作/提示卡

### 3.7 编辑器弹层

- 遮罩 `rgba(0,0,0,0.35)` + 毛玻璃
- 面板 400px、圆角 18px、`rise` 进入动画
- 字段：标题 / 分类 / 颜色点 / 日期 / 时间 / 提醒开关 / Markdown 备注（双栏编辑+预览）/ 附件 chips

---

## 4. 页面清单（30 页）

### 4.1 引导与连接（3 页）

| 页面 | 文件名 | 场景 |
| --- | --- | --- |
| 欢迎引导 | `welcome.html` | 首次使用功能引导 + 进入按钮 |
| 邮件连接 | `login.html` | Exchange 配置表单 + 登录 |
| 关于 | `about.html` | 版本信息 + 设置入口 |

### 4.2 剪切板模块（4 页）

| 页面 | 文件名 | 场景 |
| --- | --- | --- |
| 主面板 | `clipboard.html` | 搜索 + 固定卡片 + 最近列表 |
| 批量管理 | `clipboard-manage.html` | 选择 / 全选 / 删除 / 详情面板 |
| 空态 | `clipboard-empty.html` | 无记录引导 + Ctrl+C 提示卡 |
| 控件演示 | `controls.html` | 下拉 / 分段 / 开关 / 步进器 |

### 4.3 邮件模块（6 页）

| 页面 | 文件名 | 场景 |
| --- | --- | --- |
| 邮件列表 | `email.html` | 收件箱 + 状态 + 分段筛选 |
| 邮件详情 | `mail-detail.html` | 出货摘要 + 字段 + 附件 |
| 连接诊断 | `mail-doctor.html` | 8 项检查结果 |
| 字段提取 | `extractor.html` | Excel 表格解析分页 |
| 附件表格窗 | `attachment.html` | 窗口化表格视图 |
| 附件弹层 | `attach-popover.html` | 便签附件管理 |

### 4.4 便签模块（7 页）

| 页面 | 文件名 | 场景 |
| --- | --- | --- |
| 便签列表 | `notes.html` | 分类筛选 + 卡片列表 |
| 搜索创建 | `notes-search.html` | 自然语言解析（`明天9点交报告 #工作`） |
| 编辑器 | `note-editor.html` | 富编辑 + 颜色 + 附件 + 提醒 |
| 拖拽排序 | `note-sort.html` | 可交互拖拽 + 让位动画 |
| 浮动便签 | `float-note.html` | 桌面独立浮窗 |
| 迷你模式 | `mini-mode.html` | 220px 窄窗卡片 |
| 提醒通知 | `reminder.html` | 到点系统通知卡片 |

### 4.5 设置模块（5 页）

| 页面 | 文件名 | 场景 |
| --- | --- | --- |
| 设置主面板 | `settings.html` | 透明度 / 分组 / 选项 |
| 设置子面板 | `settings-subnav.html` | 分类筛选 / 主题二级导航 |
| 快捷键 | `shortcuts.html` | 组合键录制 / 冲突提示 |
| 显示预设 | `display-presets.html` | 常规 / 清晰 / 演示 |
| 桌面视觉 | `whiteboard.html` | 空白编辑板 |

### 4.6 系统与动效（5 页）

| 页面 | 文件名 | 场景 |
| --- | --- | --- |
| 托盘动效 v1 | `tray-motion.html` | 菜单 / 通知 / 时间线 |
| 托盘动效 v2 | `tray-motion-v2.html` | 细化版：可手动开合菜单、通知滑入滑出 |
| 边缘吸附 | `edge-dock.html` | 贴边 4px / 收纳 / 唤出 |
| 鼠标穿透 | `pass-through.html` | 穿透窗口 + 恢复入口 |
| 错误回退 | `error-fallback.html` | 渲染异常兜底 |

---

## 5. 交互规范

### 5.1 反馈层级

| 反馈类型 | 实现 |
| --- | --- |
| 按压反馈 | 所有可点击元素 `:active scale(0.95-0.98)`，即时于 pointer-down |
| 悬停反馈 | 仅 `@media (hover:hover) and (pointer:fine)` 生效，防止触屏误触发 |
| 焦点反馈 | `:focus-visible` 统一 2px 品牌色 outline + offset |
| 键盘可达 | 弹层可 Esc 关闭，行级操作 `:focus-within` 揭示 |

### 5.2 动效规格

| 场景 | 时长 | 曲线 |
| --- | --- | --- |
| 托盘菜单展开 | 150ms | `cubic-bezier(.32,.72,0,1)`，origin 锚定触发器 |
| 菜单项级联 | 30ms stagger | 同上 |
| 通知滑入 | 280ms | 可中断 transition，从右滑入 |
| 通知滑出 | 200ms | 同向滑出（空间一致性） |
| 徽标弹跳 | 400ms | `scale(.5)+opacity` 起步（禁止从 `scale(0)`） |
| 拖拽让位 | 180ms | 弹簧曲线 |
| 卡片完成/取消 | 450ms | 划线生长 + 淡入淡出 |

### 5.3 拖拽排序手感

- Pointer Events + `setPointerCapture`，1:1 跟随指针
- 尊重抓取偏移（grab offset）
- 5px 位移阈值激活
- 释放时按速度判定（>200px/s 即触发重排）
- 落位 FLIP 动画；`prefers-reduced-motion` 下直接重排

### 5.4 无障碍

- 全部交互元素 `aria-label` / `title`
- `prefers-reduced-motion`：所有动画降至 0.01ms
- 对比度满足 WCAG AA（主文本/背景 ≥ 4.5:1）
- 深色主题完整适配

---

## 6. 技术说明

- 单文件 HTML 原型，无构建依赖，本地资源直接引用
- 图标：Lucide（本地 `assets/lucide-1.8.0.min.js`）
- 样式：CSS 变量 Token + 静态 critical-layout（不依赖 Tailwind 运行时即可稳定渲染）
- 交互增强：原生 JS（拖拽、动效演示、弹层）
- 页面可直接双击打开，或通过导航画廊 `index.html` 浏览全部 30 页
